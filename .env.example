DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
BETTER_AUTH_SECRET="generate-with-openssl-rand-base64-32"
BETTER_AUTH_URL="http://localhost:3000"

# Optional: comma-separated list of additional origins allowed to call the
# auth API (BETTER_AUTH_URL is already trusted by default). Useful for a
# custom production domain or preview deployments.
TRUSTED_ORIGINS=""

# Optional: enables the Google button when both values are configured.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# SMTP configuration. Use the credentials provided by your email service.
SMTP_HOST="smtp.your-provider.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASSWORD="your-smtp-password"
EMAIL_FROM="Auth Starter <your-email@example.com>"
